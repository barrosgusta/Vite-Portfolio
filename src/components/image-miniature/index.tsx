import { ImageMiniatureRoot } from './ImageMiniatureRoot';
import { ImageMiniatureDescription } from './ImageMiniatureDescription';

export const ImageMiniature = {
    Root: ImageMiniatureRoot,
    Description: ImageMiniatureDescription,
}